/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;


// /** @type {import('next').NextConfig} */
// const nextConfig = {
//     async rewrites() {
//         return [
//             {
//                 source: '/api/:path*', // เส้นทางต้นทางที่ต้องการทำ proxy
//                 destination: 'http://localhost:3002/:path', // URL ปลายทางที่ต้องการเปลี่ยนไป
//             },
//         ]
//     },
// };

// export default nextConfig;
